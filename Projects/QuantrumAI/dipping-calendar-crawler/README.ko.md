# Dipping - 금융 캘린더 크롤러 풀스택 구현

## 🎯 프로젝트 배경

**문제점**: 주식 투자자들이 중요한 경제지표 발표 일정을 놓치면 급격한 주가 변동에 대응하지 못함

**해결책**: Investing.com에서 금융 이벤트 데이터를 크롤링하여, 사용자 친화적인 캘린더 UI로 제공

**핵심 가치**: "중요한 경제 이벤트를 놓치지 말자"

---

## 🛠 기술 스택

### 백엔드
- **Python 3.x**: 크롤링 및 백엔드 로직
- **Selenium + undetected-chromedriver**: 동적 웹 크롤링
- **BeautifulSoup4**: HTML 파싱
- **Django 5.1**: 백엔드 프레임워크
- **Django REST Framework**: API 서버
- **PostgreSQL**: 데이터베이스
- **APScheduler**: 자동 스케줄링

### 프론트엔드
- **React 19**: UI 프레임워크
- **TypeScript**: 타입 안전성
- **AJAX (fetch API)**: 비동기 데이터 통신

---

## 💻 내 역할

**팀 내 금융 캘린더 크롤러 전담 개발**
- 이 기능은 QuantrumAI 팀 내에서 제가 처음부터 끝까지 담당했습니다
- 웹 크롤러 구현 (Selenium + Anti-Bot 우회)
- Django 백엔드 개발 (ORM, API, 스케줄링)
- React + TypeScript 프론트엔드 개발
- 전체 데이터 흐름 설계 및 통합

---

## 🔧 핵심 구현 내용

### 1. 웹 크롤링 (Selenium)

#### A. Anti-Bot 우회 전략
**문제**: Investing.com의 봇 차단 시스템

**해결책**:
```python
import undetected_chromedriver as uc

options = uc.ChromeOptions()
options.add_argument('--user-agent=Mozilla/5.0...')
driver = uc.Chrome(options=options)
```

- `undetected-chromedriver` 사용
- `navigator.webdriver = true` 속성 숨김
- User-Agent 변경

#### B. 팝업 처리 (200초 절약)
**❌ 실패한 방법**:
```python
# 팝업이 뜰 때까지 기다려서 클릭
wait.until(EC.element_to_be_clickable((By.ID, "popup")))
popup.click()  # 느림!
```

**✅ 성공한 방법**:
```python
# JavaScript로 DOM에서 팝업 요소 강제 제거
driver.execute_script("""
    document.querySelector('.popup').remove();
    document.querySelector('.general-overlay').remove();
""")
```

→ **핵심 배움**: 클릭 대기보다 DOM 제거가 훨씬 빠름

#### C. jQuery UI Datepicker 조작 (프로젝트 최대 난관)

**문제**: Investing.com의 날짜 필터가 jQuery UI Datepicker로 구현됨

**❌ 실패한 시도들**:
```python
# 1. input의 value 직접 설정 (화면엔 보이지만 내부 상태 업데이트 안 됨)
driver.execute_script("$('#dateInput').val('2025-10-01')")

# 2. 단순 클릭 (범위 선택 안 됨)
date_element.click()
```

**✅ 성공한 방법 - 사용자 시퀀스 모방**:
```python
# 1. 시작 날짜 클릭 (1일)
start_date = driver.find_element(By.XPATH, "//a[text()='1']")
start_date.click()
time.sleep(1)

# 2. 종료 날짜 클릭 (31일)
end_date = driver.find_element(By.XPATH, "//a[text()='31']")
end_date.click()
time.sleep(1)

# 3. 다시 시작 날짜 클릭 (범위 확정)
start_date.click()
time.sleep(1)

# 4. Apply 버튼 클릭 (onClose 이벤트 트리거)
apply_button = driver.find_element(By.ID, "applyBtn")
apply_button.click()
```

**핵심 깨달음**:
> "값 설정 ≠ 사용자 액션"
> jQuery UI의 내부 상태는 실제 클릭 이벤트로만 업데이트됨

**왜 이 시퀀스가 필요한가?**
- jQuery UI Datepicker는 "시작일 → 종료일 → 시작일" 클릭으로 범위를 확정
- `applyBtn` 클릭 시 `onClose` 이벤트가 트리거되어 날짜 적용

#### D. 안정성 확보

- Explicit Wait로 요소 로딩 대기
- 재시도 로직 (최대 3회)
- 안전한 파싱 (try/except)

---

### 2. Django 백엔드

#### 핵심 구현

**모델 설계**: DateField + TimeField 분리 (날짜별 필터링 용이)
- `unique_together`로 중복 방지

**DB 저장**: `update_or_create`로 효율적 저장
- 데이터 있으면 업데이트, 없으면 생성

**API**: DRF ViewSet으로 동적 필터링
- `/api/events/?country=US` (국가별 조회)

**자동 스케줄링**: APScheduler
- 매달 마지막 날 23시 자동 크롤링

---

### 3. React + TypeScript 프론트엔드

**타입 정의**: EconomicEvent 인터페이스로 타입 안전성 확보

**API 서비스**: 서비스 레이어 분리로 재사용성 증가

**데이터 가공**: 날짜별 그룹화 + D-Day 계산

---

## 📊 전체 데이터 흐름

```
[Investing.com]
       ↓ (Selenium 크롤링)
   [크롤러]
       ↓ (ORM)
 [PostgreSQL]
       ↓ (Django API)
  [JSON 응답]
       ↓ (fetch)
[React 프론트]
       ↓ (렌더링)
   [사용자]
```

---

## 💡 배운 점

### 기술적 배움

**1. 웹 크롤링의 복잡성**
- Anti-Bot 우회는 지속적인 전쟁
- jQuery UI 내부 구조 이해의 중요성
- DOM 조작 vs 사용자 시퀀스 모방

**2. Django 설계 원칙**
- 분리 가능한 필드는 분리 (Date + Time)
- `unique_together`로 DB 레벨 중복 방지
- `update_or_create`로 원자적 저장

**3. TypeScript의 가치**
- 타입은 살아있는 문서
- 컴파일 타임 에러로 런타임 안정성 확보

### 문제 해결 방법론

**쪼개기 (Divide and Conquer)**:
1. 크롤링만 (TXT 저장)
2. DB 저장 테스트
3. API 엔드포인트 구축
4. 프론트 연동

---

## 🔗 관련 개념

- [Selenium 크롤링 기술](../../Concepts/selenium-crawling-techniques/)
- [Django 백엔드 패턴](../../Concepts/django-backend-patterns/)
- [React TypeScript 기초](../../Concepts/react-typescript-basics/)
- [Git 기초](../../Concepts/git-essentials/)
- [디버깅 전략](../../Concepts/debugging-strategies/)
- [개발자 마인드셋](../../Concepts/developer-mindset/)

---

## ❓ 면접 예상 질문

**Q1: jQuery UI Datepicker를 어떻게 조작했나요?**
> A: JavaScript로 value를 직접 설정하면 화면엔 보이지만 내부 상태가 업데이트되지 않습니다. 실제 사용자처럼 "시작일 클릭 → 종료일 클릭 → 시작일 다시 클릭 → Apply 버튼"이라는 시퀀스를 Selenium으로 재현했습니다. onClose 이벤트가 트리거되어야 날짜가 실제로 적용됩니다.

**Q2: Anti-Bot을 어떻게 우회했나요?**
> A: undetected-chromedriver를 사용해 navigator.webdriver 속성을 숨기고, User-Agent를 변경했습니다. 일반 Selenium은 navigator.webdriver = true로 탐지되지만, undetected-chromedriver는 이를 숨겨 사람처럼 위장합니다.

**Q3: DateTimeField 대신 DateField + TimeField를 선택한 이유는?**
> A: 날짜별 필터링이 매우 빈번한 요구사항이었습니다. DateTimeField는 날짜만 필터링하려면 날짜 범위(00:00~23:59)를 지정해야 하지만, 분리하면 `filter(date='2025-10-20')`처럼 간단합니다.

**Q4: API 서비스 레이어를 분리한 이유는?**
> A: 컴포넌트에서 fetch를 직접 호출하면 API URL이나 로직이 변경될 때 모든 컴포넌트를 수정해야 합니다. 서비스 레이어로 분리하면 한 곳만 수정하면 되고, 재사용성도 높아집니다.

**Q5: APScheduler를 선택한 이유는?**
> A: Celery는 Redis/RabbitMQ 같은 메시지 브로커가 필요해 인프라가 복잡해집니다. APScheduler는 Django 서버와 함께 백그라운드 스레드로 동작해 간단하고, 월말 23시 같은 복잡한 스케줄도 CronTrigger로 쉽게 설정할 수 있습니다.

---

## 📂 프로젝트 구조

```
dipping-calendar/
├── backend/
│   ├── crawler/
│   │   ├── investing_crawler.py
│   │   └── scheduler.py
│   ├── calendar_app/
│   │   ├── models.py
│   │   ├── serializers.py
│   │   ├── views.py
│   │   └── apps.py
│   └── manage.py
└── frontend/
    ├── src/
    │   ├── components/
    │   │   └── CalendarTab.tsx
    │   └── api/
    │       └── services/
    │           └── calendarService.ts
    └── package.json
```

---

**현재 상태**: ✅ 완료 (배포 준비 중)
**담당**: 바다 (QuantrumAI 팀 내 금융 캘린더 크롤러 전담)
